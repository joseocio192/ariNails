version: '3'  # This line is being removed

services:
  backend: 
    build: ./backend
    container_name: my_backend
    restart: always
    env_file:
      - ./.env
    depends_on:
      - postgres
    ports:
      - "3000:3000"

  # Frontend service (disabled)
  # frontend: 
  #   build: ./frontend
  #   container_name: frontend
  #   restart: always
  #   env_file:
  #     - ./.env
  #   depends_on:
  #     - backend
  #   ports:
  #     - "443:443"

  postgres:
    image: postgres:16
    container_name: postgres
    restart: always
    env_file:
      - ./.env
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  db_data:
